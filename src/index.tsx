import { set, get, remove } from "./utils/storage.utils";
import {
  usePersistLocalVariable,
  persist,
} from "./hooks/usePersistLocalVariable";

export { set, get, remove, persist, usePersistLocalVariable };
